# ───────────────────────────────────────────────────────────────
# COPYRIGHTED COURSE MATERIAL — never commit to a public repo.
# The Global Dev Experts slides, recordings and summaries are
# "All Rights Reserved". Keep them OUT of version control.
# ───────────────────────────────────────────────────────────────
*.pdf
*.docx
*.pptx
*.mp4
*.zip
Week */
Zip_bk_up/
**/*Recording*
**/*Deck*
**/*Summary*

# Allow my own exported diagrams/PDFs if I ever want them (opt-in):
!docs/**/*.png
!docs/**/*.svg

# ───────────────────────────────────────────────────────────────
# MkDocs / docs site build output
# ───────────────────────────────────────────────────────────────
site/
.cache/

# ───────────────────────────────────────────────────────────────
# Python (for mkdocs / tooling)
# ───────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/

# ───────────────────────────────────────────────────────────────
# Node / reference implementation (if added later)
# ───────────────────────────────────────────────────────────────
node_modules/
dist/
build/
target/
*.log

# ───────────────────────────────────────────────────────────────
# OS / editor cruft
# ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# ───────────────────────────────────────────────────────────────
# Secrets and credentials — never commit.
# Nothing here belongs in the repository at any point.
# ───────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.p12
*.jks
*.keystore
*.ppk
id_rsa*
id_dsa*
id_ecdsa*
id_ed25519*
.ssh/
.aws/
.npmrc
.netrc
credentials
credentials.json
secrets.json
service-account*.json
*.tfvars
!*.tfvars.example
