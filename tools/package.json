{
  "name": "neobank-hld-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Builds the single-file submission document from the design sources.",
  "scripts": {
    "build": "node build-submission.mjs"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.4.0",
    "docx": "^9.7.1",
    "marked": "^15.0.0"
  }
}
